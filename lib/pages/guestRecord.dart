import 'package:flutter/material.dart';

class GuestRecord extends StatelessWidget {
  const GuestRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Abcd");
  }
}
